<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container">
  <%= link_to "Recruitment", root_path, class: "navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
     <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
      <li class= "nav-item">
      <% if user_signed_in? %>
        <%= link_to "admin", admin_index_path if current_user.admin? %>
               <%= link_to "Joboffers", joboffers_path %>
                   <%= link_to "Mis Pstulaciones", candidates_path %>   
        <%else%>
                Inicia sesión o registrate
            <% end %>
          </li>
      </ul>
 <ul class="navbar-nav ms-auto mb-5 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">

          <% if user_signed_in? %> 
              <%  if current_user.admin? %>
                <div class="icon-badge-group">
                    <div class="icon-badge-container">
                        <i class="far fa-envelope-open icon-badge-icon"></i>
                        <div class="icon-badge"><%= Reaction.all.count %></div>
                    </div>
                </div>
                <% end %>

               <%= current_user.nickname %>    
            <% if current_user.avatar.persisted? %>
              <%=  image_tag current_user.avatar, class:'rounded-circle', height: "40px", width: "40px"  %> 
            <% else%>
               <img src="https://placehold.co/40x40" alt="Avatar" class='rounded-circle', height="40px", width="40px" >

            <% end %>

          <% else %> Dale registrate o conectate 
          <% end %> </a>
          <ul class="dropdown-menu">
          <% if user_signed_in? %>
              <li>
              <%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item',method: :delete %>
              </li>
              <li>
              <%= link_to 'Editar Perfil', edit_user_registration_path, class: 'dropdown-item'%>

              </li>
            
            <% else %> 
              <li>
              <%= link_to 'Iniciar sesión', new_user_session_path, class: 'dropdown-item'%>
              </li>
              <li>
              <hr class="dropdown-divider">
              </li>
            <li><%= link_to 'Registro', new_user_registration_path, class: 'dropdown-item'%> </li><% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>




